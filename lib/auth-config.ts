/**
 * Shared auth configuration constants.
 * This file can be imported by both client and server code.
 */

/**
 * Allowed email domain for signup.
 * Only emails ending with @{ALLOWED_EMAIL_DOMAIN} can create accounts.
 */
export const ALLOWED_EMAIL_DOMAIN = 'fintoc.com';
